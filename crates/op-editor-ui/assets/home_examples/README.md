# Home example art assets

`coffee-demo.jpg` — 360×540 demo photograph used only inside the Studio home's
App-example phone mock-ups (hero image + menu thumbnails). It is generated
concept material carried over from the design prototype
(`openpencil-docs/prototypes/entry-home-studio/assets/coffee.png`, resized and
re-encoded here); it is NOT an OpenPencil generation result and must never be
presented as one. Keep the folder under 250 KB (desktop bundle size gate).
